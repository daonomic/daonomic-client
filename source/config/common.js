export const { realm } = window.config;
export const sale = window.config.sale[process.env.NODE_ENV];
export const { contactEmail } = window.config;
export const { termsOfServiceURL } = window.config;
